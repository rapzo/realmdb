{
  "name": "realmdb",
  "packageManager": "yarn@3.6.1",
  "private": true,
  "scripts": {
    "api:dev": "yarn workspace @realmdb/api dev",
    "app:dev": "yarn workspace @realmdb/app dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.44.0",
    "prettier": "^3.0.0"
  }
}
